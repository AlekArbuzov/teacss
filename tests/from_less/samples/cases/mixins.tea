.mixin(border) { border: @{border}px solid black; }
.mixout() { border-color: orange; }
.borders() {
    .inner_color(col){
        color: @col;
    }
    border-style: dashed;
}

#container {
  .mixin(1);
  .mixout();
  .borders();
  .inner_color('red');
}

