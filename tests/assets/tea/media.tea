@ teavar = 42;

@ var _media = "media";

@@{"media"} print {
    .class {
        color: blue;
        .sub {
            width: @teavar;
        }
    }
}

@@{_media} screen {
    @ base = 8;
    body { max-width: @{base * 60}; }
}
